import { createSlice } from '@reduxjs/toolkit'
import { homeReducer } from './index';



export const homeReducer = createSlice({
  name: 'home',
  initialState: {
    categories: [],
  },
  reducers: {

  }
})

export default function homeReducer.reducer

